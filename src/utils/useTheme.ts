import { createTheme } from "@mui/material/styles";

const theme = createTheme({
    palette: {
        primary: {
            main: "#FFFFF0",
        },
        secondary: {
            main: "#F0F0FF",
        },
         text: {
            primary: "#000000", 
    },
    },
});

export default theme;